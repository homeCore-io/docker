# Generic thermostat plugin.
# Uncomment under `include:` in compose.yml to enable.
#
# Setup:
#   mkdir hc-thermostat-data    # owner = your user; entrypoint will match
#   docker compose up -d
#   First boot creates ./hc-thermostat-data/config.toml. Edit, restart.
services:
  hc-thermostat:
    image: ghcr.io/homecore-io/hc-thermostat:latest
    container_name: hc-thermostat
    restart: unless-stopped
    pull_policy: always
    network_mode: host
    environment:
      RUST_LOG: info
    volumes:
      - ./hc-thermostat-data:/homecore
